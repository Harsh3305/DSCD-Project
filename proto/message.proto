syntax = "proto3";

package mapper;

service Mapper {
  // Unary
  rpc SendFileLocation (MapperRequest) returns (MapperReply);

}
message MapperRequest {
  string file_name = 1;
}
message MapperReply {
}
